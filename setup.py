from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='building an nlp model to classify short texts as funny or not.',
    author='Soffie',
    license='',
)
